./scripts/install $@

sleep 5
./scripts/smoketest
if [ $? -ne 0 ]
then
  exit 1
fi

./scripts/unittest

# add some data to the database
docker exec -it database bash -c 'psql --file=data/dev/behaviour_test_data.sql -d "$DATABASE_NAME"'

./scripts/apitest
./scripts/behaviourtest
./scripts/lint
