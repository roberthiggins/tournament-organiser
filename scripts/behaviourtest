# add some data to the db
# TODO this should be moved into the behaviourtest script once the unit_tests
# no longer need it
docker exec -it db bash -c 'psql --file=data/dev/behaviour_test_data.sql -d "$DB_NAME"'

# We use behat and a selnium server to run behaviour tests
sudo docker kill selenium behat         &> /dev/null
sudo docker rm -v selenium behat        &> /dev/null

pushd behat
sudo docker build -t behat_image .
popd

sudo docker run --name=selenium -d -p 4444:4444 selenium/standalone-firefox:2.48.2
sudo docker run --name=behat -t --link=selenium --link=webserver --link=daoserver behat_image

